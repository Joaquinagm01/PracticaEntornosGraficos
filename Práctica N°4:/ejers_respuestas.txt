Práctica N°4 - Respuestas

PHP: variables, tipos, operadores, expresiones, estructuras de control
PHP: arrays, funciones

Ejercicio 1

Variables y tipo:
- $a: boolean
- $b: string
- $c: string
- $d: integer
- $f: integer
- $g: integer
- $i: parámetro de la función doble, numérico

Operadores:
- = asignación
- * multiplicación
- += suma y asignación
- ?: operador ternario
- ++ preincremento y postincremento

Funciones y parámetros:
- doble($i): recibe un valor numérico y devuelve $i * 2
- gettype($x): devuelve el tipo de la variable
- is_int($d): verifica si $d es entero
- is_string($a): verifica si $a es cadena

Estructuras de control:
- if
- if con condición falsa
- operador ternario

Salida por pantalla, sin contar posibles notices y sin separadores entre echo:
booleanstringstringinteger1xyzxyz184444

Ejercicio 2

a) Sí, son equivalentes en salida. Los tres imprimen:
12345678910

b) Sí, también son equivalentes en salida. Los cuatro imprimen:
12345678910

c) Sí, son equivalentes para los valores 0, 1 y 2. En ambos casos se imprime el mismo texto según el valor de $i.

Ejercicio 3

a) El código genera una tabla HTML de 5 filas por 2 columnas, todas con celdas vacías.

b) El código muestra un formulario para cargar la edad. Si se envía el formulario, compara la edad ingresada y muestra:
- "Mayor de edad" si age >= 21
- "Menor de edad" en caso contrario

Ejercicio 4

Primero se ejecuta el echo antes del include, por lo que $flor y $color todavía no están definidos y se imprimen vacíos. Luego se incluye datos.php y recién ahí las variables toman los valores:
- $color = blanco
- $flor = clavel

Salida esperada, ignorando notices por variables no definidas:
El   
 El clavel blanco

Ejercicio 5

contador.php lee el valor guardado en contador.dat, lo incrementa en 1, lo vuelve a guardar y muestra la cantidad total de visitas en pantalla.

visitas.php solo incluye contador.php dentro del cuerpo de la página.

Para que funcione correctamente, contador.dat debe existir, tener un valor inicial numérico y permisos de lectura y escritura.

PHP: arrays, funciones

Ejercicio 1

Sí, los dos códigos son equivalentes. Ambos crean el mismo arreglo asociativo con los mismos elementos y el mismo valor final en la posición siguiente disponible.

Ejercicio 2

a) bar1

b) 5942

c) No produce salida por pantalla. Solo modifica y luego elimina el arreglo.

Ejercicio 3

a) getdate() devuelve un arreglo con la fecha y hora actual. El echo arma un texto del tipo:
Has entrado en esta pagina a las HH horas, con MM minutos y SS segundos, del D/M/A

b) La función sumar(5,6) muestra:
5+6=11

Ejercicio 4

La función comprobar_nombre_usuario valida nombres de usuario:
- longitud entre 3 y 20 caracteres
- solo permite letras, números, guion y guion bajo

Devuelve true si el nombre es válido y false si no lo es.

Script de prueba:

<?php
function comprobar_nombre_usuario($nombre_usuario){
   if (strlen($nombre_usuario) < 3 || strlen($nombre_usuario) > 20){
      echo $nombre_usuario . " no es válido<br>";
      return false;
   }

   $permitidos = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789-_";
   for ($i = 0; $i < strlen($nombre_usuario); $i++){
      if (strpos($permitidos, substr($nombre_usuario, $i, 1)) === false){
         echo $nombre_usuario . " no es válido<br>";
         return false;
      }
   }

   echo $nombre_usuario . " es válido<br>";
   return true;
}

comprobar_nombre_usuario("juan");
comprobar_nombre_usuario("ab");
comprobar_nombre_usuario("juan_perez");
comprobar_nombre_usuario("usuario!");
?>